FROM node:16

ARG MONGODB_URI

WORKDIR /usr/src/app

COPY --chown=node:node . .

RUN npm ci

ENV MONGODB_URI=${MONGODB_URI}
ENV SECRET=${SECRET}
ENV PORT=${PORT}
ENV TEST_MONGODB_URI=${TEST_MONGODB_URI}

USER node

CMD npm start